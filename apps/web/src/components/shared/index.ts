// What the app draws wherever it is needed rather than in one feature alone. Each component is
// a directory of its own, and is reached through this one rather than around it
export * from "./sidebar";
