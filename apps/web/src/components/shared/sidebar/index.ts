// The sidebar is what the directory is for, so it is what leaves it. The shape of what it is
// given goes with it, since nothing can draw one without saying what it holds
export { default as Sidebar } from "./Sidebar";
export * from "./Sidebar.types";
