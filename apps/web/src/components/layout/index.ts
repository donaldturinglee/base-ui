// The layout is what the directory is for, so it is what leaves it. The row across the top and
// the column of links are parts of it rather than pieces in their own right, and neither is drawn
// anywhere else, so both are reached through it rather than around it
export { default as Layout } from "./Layout";
