// What stands around every page rather than belonging to any one of them. The row across the top
// and the column of links are parts of the layout, so it is the layout that leaves here and they
// are reached through it rather than around it
export * from "./layout";
