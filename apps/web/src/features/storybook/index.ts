// A page to a link in the column beside it, so what leaves the feature is the page itself, and
// beside it the address the Storybook is read at, which the pages linking out to it are handed
// rather than each writing out again
export { default as Storybook, storybookUrl } from "./Storybook";
