// A page to a link in the column beside it, so what leaves the feature is the pages themselves.
// A guide is read through from the top rather than looked up the way a component page is, which
// is what holds them together here rather than among the components
export { default as MCPServer } from "./MCPServer";
