// What the site answers with when it cannot answer with what was asked for. There is one of them
// so far and there will be more, so they are held together by what they are for rather than each
// standing as a feature of its own
export { default as NotFound } from "./NotFound";
