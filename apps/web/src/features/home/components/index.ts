// The sections the page is assembled from. They are the page's own, so they are reached
// through it rather than around it, and this is what it reaches them by
export { default as HomeHeader } from "./HomeHeader";
export { default as HomeHero } from "./HomeHero";
export { default as HomeSidebar } from "./HomeSidebar";
