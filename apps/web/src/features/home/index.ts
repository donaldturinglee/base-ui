// The page is what the feature is for, so it is what leaves it. There is the one of it, and it is
// written as the one file rather than assembled from sections held beside it
export { default as Home } from "./Home";
