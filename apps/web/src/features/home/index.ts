// The page is what the feature is for, so it is what leaves it. The sections it is assembled
// from are its own, and are reached through it rather than around it
export { default as Home } from "./Home";
