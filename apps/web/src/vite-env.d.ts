// What Vite hands the app beyond the modules it was written with: the stylesheets and assets
// that are imported for their effect rather than for a value, and `import.meta.env`. None of it
// is declared by the sources themselves, so the types come from Vite's own client declarations
/// <reference types="vite/client" />
